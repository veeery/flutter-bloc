import 'package:flutter/material.dart';

class CounterPage4 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
      appBar: AppBar(
        title: const Text('Counter Page 4'),
      ),
      body: Column(
        children: [
          Text('ssssss')
        ],
      ),
    );
  }
}
